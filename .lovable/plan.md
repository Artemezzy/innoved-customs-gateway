

# План: Блок "Наши достижения" с анимацией счётчика

## Обзор
Удаление текущей статистики из Hero и создание отдельного блока "Наши достижения" под видеорядом с анимированными счётчиками чисел.

## Изменения

### 1. Очистка Hero
**Файл:** `src/components/Hero.tsx`
- Удаление блока Stats Bar (строки 59-75)
- Удаление данных stats из объекта content

### 2. Создание компонента Achievements
**Файл:** `src/components/Achievements.tsx` (новый)

**Данные:**
| Число | Описание (RU) | Описание (EN) |
|-------|---------------|---------------|
| 2400+ | оформленных деклараций за 2025 год | declarations processed in 2025 |
| 115+ | постоянных заказчиков | regular customers |
| 80+ | товарных групп | product categories |

**Функционал:**
- Хук `useCountUp` для анимации чисел от 0 до целевого значения
- Easing-функция `easeOutCubic` для замедления в конце
- Длительность: 2.5 секунды
- Запуск анимации при появлении в viewport (Intersection Observer)
- Двуязычная поддержка (RU/EN)

### 3. Интеграция на главную
**Файл:** `src/pages/Home.tsx`
- Добавление компонента Achievements между Hero и Testimonials

---

## Варианты оформления

### Вариант A: Минималистичные карточки
```text
┌──────────────────────────────────────────────────────────────────┐
│                      НАШИ ДОСТИЖЕНИЯ                             │
├──────────────────┬──────────────────┬───────────────────────────┤
│                  │                  │                           │
│     2400+        │      115+        │          80+              │
│   деклараций     │   заказчиков     │     товарных групп        │
│   за 2025 год    │   постоянных     │                           │
│                  │                  │                           │
└──────────────────┴──────────────────┴───────────────────────────┘
```
- Белый фон (`bg-background`)
- Числа крупным шрифтом синего цвета (`text-primary`)
- Подписи серым цветом (`text-muted-foreground`)
- Без рамок, разделение пространством

### Вариант B: Цветной акцентный блок
```text
┌──────────────────────────────────────────────────────────────────┐
│  ███████████████████████████████████████████████████████████████ │
│  █                    НАШИ ДОСТИЖЕНИЯ                         █ │
│  █  ┌─────────┐     ┌─────────┐     ┌─────────┐              █ │
│  █  │ 2400+   │     │  115+   │     │   80+   │              █ │
│  █  │ декл.   │     │ заказч. │     │ групп   │              █ │
│  █  └─────────┘     └─────────┘     └─────────┘              █ │
│  ███████████████████████████████████████████████████████████████ │
└──────────────────────────────────────────────────────────────────┘
```
- Синий градиентный фон (`bg-gradient-primary`)
- Белые числа и текст
- Карточки с полупрозрачным фоном

### Вариант C: С иконками
```text
┌──────────────────────────────────────────────────────────────────┐
│                      НАШИ ДОСТИЖЕНИЯ                             │
│                                                                   │
│   📋 2400+           👥 115+           📦 80+                     │
│   деклараций         заказчиков        товарных групп             │
│   за 2025 год        постоянных                                   │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```
- Иконки Lucide над каждым числом (FileText, Users, Package)
- Светлый фон с лёгкой тенью на карточках
- Hover-эффект на карточках

### Вариант D: Горизонтальная полоса
```text
══════════════════════════════════════════════════════════════════
     2400+              │        115+              │       80+      
   деклараций           │     заказчиков           │   товарных     
   за 2025 год          │     постоянных           │     групп      
══════════════════════════════════════════════════════════════════
```
- Компактный дизайн без заголовка
- Вертикальные разделители между показателями
- Фон `bg-muted`

---

## Рекомендация

Предлагаю реализовать **Вариант C** (с иконками) как основной - он сочетает современный вид, профессиональность и хорошую читаемость. Иконки помогают быстро понять значение каждой метрики.

---

## Технические детали

### Анимация счётчика
```typescript
const easeOutCubic = (t: number) => 1 - Math.pow(1 - t, 3);

function useCountUp(target: number, duration = 2500) {
  const [count, setCount] = useState(0);
  const ref = useRef<HTMLDivElement>(null);
  
  useEffect(() => {
    const observer = new IntersectionObserver((entries) => {
      if (entries[0].isIntersecting) {
        // Запуск анимации
        const startTime = performance.now();
        const animate = (currentTime: number) => {
          const elapsed = currentTime - startTime;
          const progress = Math.min(elapsed / duration, 1);
          const easedProgress = easeOutCubic(progress);
          setCount(Math.floor(target * easedProgress));
          
          if (progress < 1) {
            requestAnimationFrame(animate);
          }
        };
        requestAnimationFrame(animate);
      }
    });
    
    if (ref.current) observer.observe(ref.current);
    return () => observer.disconnect();
  }, [target, duration]);
  
  return { count, ref };
}
```

### Стилизация (Вариант C)
- Секция: `py-16 bg-background`
- Заголовок: `text-3xl font-bold text-foreground text-center mb-12`
- Сетка: `grid grid-cols-1 md:grid-cols-3 gap-8`
- Карточка: `bg-card p-8 rounded-lg shadow-card text-center hover:shadow-hover transition-all`
- Иконка: `w-12 h-12 text-primary mx-auto mb-4`
- Число: `text-4xl md:text-5xl font-bold text-primary mb-2`
- Суффикс (+): отдельный `<span>` для корректного отображения
- Подпись: `text-muted-foreground`

